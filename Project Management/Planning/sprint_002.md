## Sprint 2: The Foyer (Home Gallery)
Goal: Build the "Exhibit Hall" where users can see the mystery collection.

Scope:

In: The Hero section, the Grid system, 3 "Placeholder" cards.

Out: Real descriptions, source links, final high-res images.

Tasks:

Code a full-screen "Hero" section with a "The Sage" style headline.

Create a CSS Grid (3 columns) for mystery cards.

Apply the Vignette and Hover effects to image containers.

Files to touch: index.html, style.css

Acceptance Criteria: Hovering over a card makes it scale slightly; images look "moody" (low saturation).

Verification Checklist:

[ ] Grid is responsive (drops to 1 column on mobile).

[ ] Images have a filter: grayscale(20%).

[ ] Cards have a "Case No." label (Authority principle).